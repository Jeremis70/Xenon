# Package Manager

No doc yet. TODO: describe package manager design, dependency manifest format, lockfile strategy, and registry model.

## Current roadmap signal

- Cargo-like tooling (`xenon new`, `xenon build`, `xenon run`) is a long-term goal.
