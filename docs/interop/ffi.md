# FFI and Interoperability

No doc yet. TODO: describe C/Rust interop, foreign type mapping, symbol export/import, and safety requirements.

## Current known related feature

- `asm { ... }` and `llvm { ... }` blocks.
- These are low-level control forms, not a complete FFI specification.
