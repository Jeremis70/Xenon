// Testing MVP for xenon programming language
fn main()->u8
{
    u32 x = 0;
    loop {
        if x == 10
        {
            break;
        }
        x+=1;
    }
    return x;
}

fn add(u32 x,u32 y)->u32 sum
{
    sum = x + y;
}
