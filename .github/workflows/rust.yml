name: Rust CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUSTFLAGS: -D warnings
  CARGO_INCREMENTAL: 0

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect code changes
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      code: ${{ steps.filter.outputs.code }}
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 2
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            code:
              - '**/*.rs'
              - '**/Cargo.toml'
              - '**/Cargo.lock'
              - 'rust-toolchain.toml'
              - '.github/workflows/rust.yml'

  licenses:
    name: License Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7.0.1

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      # Optional but helpful: caches cargo install downloads/registry
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install cargo-deny
        run: cargo install cargo-deny --locked

      - name: Check licenses
        run: cargo deny check licenses

  format:
    name: Format Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7.0.1

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: rustfmt

      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Check formatting
        run: cargo fmt --all -- --check

  clippy:
    name: Clippy Lints
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7.0.1

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: clippy

      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install LLVM 22 (Linux)
        run: |
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
          sudo add-apt-repository "deb http://apt.llvm.org/$(lsb_release -cs)/ llvm-toolchain-$(lsb_release -cs)-22 main"
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends llvm-22 llvm-22-dev libpolly-22-dev clang-22 libclang-22-dev lld-22

      - name: Configure LLVM env (Linux)
        shell: bash
        run: |
          LLVM_PATH=/usr/lib/llvm-22
          echo "LLVM_SYS_221_PREFIX=${LLVM_PATH}" >> "$GITHUB_ENV"
          echo "PATH=${LLVM_PATH}/bin:$PATH" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=${LLVM_PATH}/lib:${LD_LIBRARY_PATH:-}" >> "$GITHUB_ENV"
          echo "LIBCLANG_PATH=${LLVM_PATH}/lib" >> "$GITHUB_ENV"

      - name: Verify llvm-config
        shell: bash
        run: |
          which llvm-config-22
          llvm-config-22 --version

      - name: Run Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      checks: write
    steps:
      - uses: actions/checkout@v7.0.1

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - name: Run security audit
        run: cargo audit

  test:
    name: Test (${{ matrix.os }} / ${{ matrix.rust }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable]
        include:
          - os: ubuntu-latest
            rust: beta
          - os: ubuntu-latest
            rust: nightly

    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v7.0.1

      # -------------------------
      # Linux/macOS: rustup toolchain
      # -------------------------
      - name: Setup Rust (Linux/macOS)
        if: runner.os != 'Windows'
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}

      - name: Cache Rust (Linux/macOS)
        if: runner.os != 'Windows'
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
          shared-key: "${{ matrix.os }}-${{ matrix.rust }}"
          save-if: ${{ github.ref == 'refs/heads/master' }}

      - name: Install LLVM 22 (Linux)
        if: runner.os == 'Linux'
        run: |
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
          sudo add-apt-repository "deb http://apt.llvm.org/$(lsb_release -cs)/ llvm-toolchain-$(lsb_release -cs)-22 main"
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends llvm-22 llvm-22-dev libpolly-22-dev clang-22 libclang-22-dev lld-22

      - name: Install LLVM 22 (macOS)
        if: runner.os == 'macOS'
        run: brew install llvm@22

      - name: Configure LLVM env (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          LLVM_PATH=/usr/lib/llvm-22
          echo "LLVM_SYS_221_PREFIX=${LLVM_PATH}" >> "$GITHUB_ENV"
          echo "PATH=${LLVM_PATH}/bin:$PATH" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=${LLVM_PATH}/lib:${LD_LIBRARY_PATH:-}" >> "$GITHUB_ENV"
          echo "LIBCLANG_PATH=${LLVM_PATH}/lib" >> "$GITHUB_ENV"

      - name: Configure LLVM env (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          LLVM_PATH="$(brew --prefix llvm@22)"
          SDKROOT="$(xcrun --sdk macosx --show-sdk-path)"

          echo "LLVM_SYS_221_PREFIX=${LLVM_PATH}" >> "$GITHUB_ENV"
          echo "PATH=${LLVM_PATH}/bin:$PATH" >> "$GITHUB_ENV"
          echo "LIBCLANG_PATH=${LLVM_PATH}/lib" >> "$GITHUB_ENV"

          echo "CC=${LLVM_PATH}/bin/clang" >> "$GITHUB_ENV"
          echo "CXX=${LLVM_PATH}/bin/clang++" >> "$GITHUB_ENV"
          echo "AR=${LLVM_PATH}/bin/llvm-ar" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_AARCH64_APPLE_DARWIN_LINKER=${LLVM_PATH}/bin/clang" >> "$GITHUB_ENV"
          echo "RUSTFLAGS=-D warnings -C link-arg=-fuse-ld=lld -C link-arg=-lc++ -C link-arg=-lc++abi" >> "$GITHUB_ENV"

          echo "SDKROOT=${SDKROOT}" >> "$GITHUB_ENV"
          echo "CFLAGS=-isysroot ${SDKROOT}" >> "$GITHUB_ENV"
          echo "CXXFLAGS=-isysroot ${SDKROOT}" >> "$GITHUB_ENV"
          echo "MACOSX_DEPLOYMENT_TARGET=14.0" >> "$GITHUB_ENV"

      - name: Verify llvm-config (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          which llvm-config-22
          llvm-config-22 --version

      - name: Verify llvm-config (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          which llvm-config
          llvm-config --version
          clang --version

      # -------------------------
      # Windows: MSYS2 toolchain ONLY (no rustup)
      # -------------------------
      - name: Setup MSYS2 (Windows)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          path-type: minimal
          install: >-
            mingw-w64-x86_64-toolchain
            mingw-w64-x86_64-rust
            mingw-w64-x86_64-llvm
            mingw-w64-x86_64-clang
            mingw-w64-x86_64-lld
            mingw-w64-x86_64-libffi
            mingw-w64-x86_64-zlib
            mingw-w64-x86_64-zstd
            mingw-w64-x86_64-libxml2

      - name: Cache Rust (Windows/MSYS2)
        if: runner.os == 'Windows'
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
          shared-key: "${{ matrix.os }}-${{ matrix.rust }}"
          save-if: ${{ github.ref == 'refs/heads/master' }}

      - name: Configure LLVM env (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: |
          echo "LLVM_SYS_221_PREFIX=/mingw64" >> "$GITHUB_ENV"
          echo "LIBCLANG_PATH=/mingw64/bin" >> "$GITHUB_ENV"
          echo "PKG_CONFIG_PATH=/mingw64/lib/pkgconfig" >> "$GITHUB_ENV"

      - name: Verify toolchain (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: |
          which gcc
          gcc --version
          which rustc
          rustc --version
          which cargo
          cargo --version
          which llvm-config
          llvm-config --version

      # -------------------------
      # Tests
      # -------------------------

      - name: Run tests (Debug) (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: cargo test --verbose --workspace --all-targets --all-features

      - name: Run tests (Debug)
        if: runner.os != 'Windows'
        run: cargo test --verbose --workspace --all-targets --all-features

  test-release:
    name: Test Release (${{ matrix.os }} / ${{ matrix.rust }})
    needs: [changes]
    if: needs.changes.outputs.code == 'true'
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable]
        include:
          - os: ubuntu-latest
            rust: beta
          - os: ubuntu-latest
            rust: nightly

    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v7.0.1

      # -------------------------
      # Linux/macOS: rustup toolchain
      # -------------------------
      - name: Setup Rust (Linux/macOS)
        if: runner.os != 'Windows'
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}

      - name: Cache Rust (Linux/macOS)
        if: runner.os != 'Windows'
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
          shared-key: "${{ matrix.os }}-${{ matrix.rust }}"
          save-if: ${{ github.ref == 'refs/heads/master' }}

      - name: Install LLVM 22 (Linux)
        if: runner.os == 'Linux'
        run: |
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
          sudo add-apt-repository "deb http://apt.llvm.org/$(lsb_release -cs)/ llvm-toolchain-$(lsb_release -cs)-22 main"
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends llvm-22 llvm-22-dev libpolly-22-dev clang-22 libclang-22-dev lld-22

      - name: Install LLVM 22 (macOS)
        if: runner.os == 'macOS'
        run: brew install llvm@22

      - name: Configure LLVM env (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          LLVM_PATH=/usr/lib/llvm-22
          echo "LLVM_SYS_221_PREFIX=${LLVM_PATH}" >> "$GITHUB_ENV"
          echo "PATH=${LLVM_PATH}/bin:$PATH" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=${LLVM_PATH}/lib:${LD_LIBRARY_PATH:-}" >> "$GITHUB_ENV"
          echo "LIBCLANG_PATH=${LLVM_PATH}/lib" >> "$GITHUB_ENV"

      - name: Configure LLVM env (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          LLVM_PATH="$(brew --prefix llvm@22)"
          SDKROOT="$(xcrun --sdk macosx --show-sdk-path)"

          echo "LLVM_SYS_221_PREFIX=${LLVM_PATH}" >> "$GITHUB_ENV"
          echo "PATH=${LLVM_PATH}/bin:$PATH" >> "$GITHUB_ENV"
          echo "LIBCLANG_PATH=${LLVM_PATH}/lib" >> "$GITHUB_ENV"

          echo "CC=${LLVM_PATH}/bin/clang" >> "$GITHUB_ENV"
          echo "CXX=${LLVM_PATH}/bin/clang++" >> "$GITHUB_ENV"
          echo "AR=${LLVM_PATH}/bin/llvm-ar" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_AARCH64_APPLE_DARWIN_LINKER=${LLVM_PATH}/bin/clang" >> "$GITHUB_ENV"
          echo "RUSTFLAGS=-D warnings -C link-arg=-fuse-ld=lld -C link-arg=-lc++ -C link-arg=-lc++abi" >> "$GITHUB_ENV"

          echo "SDKROOT=${SDKROOT}" >> "$GITHUB_ENV"
          echo "CFLAGS=-isysroot ${SDKROOT}" >> "$GITHUB_ENV"
          echo "CXXFLAGS=-isysroot ${SDKROOT}" >> "$GITHUB_ENV"
          echo "MACOSX_DEPLOYMENT_TARGET=14.0" >> "$GITHUB_ENV"

      - name: Verify llvm-config (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          which llvm-config-22
          llvm-config-22 --version

      - name: Verify llvm-config (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          which llvm-config
          llvm-config --version
          clang --version

      # -------------------------
      # Windows: MSYS2 toolchain ONLY (no rustup)
      # -------------------------
      - name: Setup MSYS2 (Windows)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          path-type: minimal
          install: >-
            mingw-w64-x86_64-toolchain
            mingw-w64-x86_64-rust
            mingw-w64-x86_64-llvm
            mingw-w64-x86_64-clang
            mingw-w64-x86_64-lld
            mingw-w64-x86_64-libffi
            mingw-w64-x86_64-zlib
            mingw-w64-x86_64-zstd
            mingw-w64-x86_64-libxml2

      - name: Cache Rust (Windows/MSYS2)
        if: runner.os == 'Windows'
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
          shared-key: "${{ matrix.os }}-${{ matrix.rust }}"
          save-if: ${{ github.ref == 'refs/heads/master' }}

      - name: Configure LLVM env (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: |
          echo "LLVM_SYS_221_PREFIX=/mingw64" >> "$GITHUB_ENV"
          echo "LIBCLANG_PATH=/mingw64/bin" >> "$GITHUB_ENV"
          echo "PKG_CONFIG_PATH=/mingw64/lib/pkgconfig" >> "$GITHUB_ENV"

      - name: Verify toolchain (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: |
          which gcc
          gcc --version
          which rustc
          rustc --version
          which cargo
          cargo --version
          which llvm-config
          llvm-config --version

      # -------------------------
      # Tests
      # -------------------------

      - name: Run tests (Release) (Windows)
        if: runner.os == 'Windows'
        shell: msys2 {0}
        run: cargo test --release --verbose --workspace --all-targets --all-features

      - name: Run tests (Release)
        if: runner.os != 'Windows'
        run: cargo test --release --verbose --workspace --all-targets --all-features

  docs:
    name: Documentation
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7.0.1

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install LLVM 22 (Linux)
        run: |
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key | sudo tee /etc/apt/trusted.gpg.d/apt.llvm.org.asc
          sudo add-apt-repository "deb http://apt.llvm.org/$(lsb_release -cs)/ llvm-toolchain-$(lsb_release -cs)-22 main"
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends llvm-22 llvm-22-dev libpolly-22-dev clang-22 libclang-22-dev lld-22

      - name: Configure LLVM env (Linux)
        shell: bash
        run: |
          LLVM_PATH=/usr/lib/llvm-22
          echo "LLVM_SYS_221_PREFIX=${LLVM_PATH}" >> "$GITHUB_ENV"
          echo "PATH=${LLVM_PATH}/bin:$PATH" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=${LLVM_PATH}/lib:${LD_LIBRARY_PATH:-}" >> "$GITHUB_ENV"

      - name: Verify llvm-config
        shell: bash
        run: |
          which llvm-config-22
          llvm-config-22 --version

      - name: Build documentation
        run: cargo doc --no-deps --workspace --all-features
        env:
          RUSTDOCFLAGS: -D warnings